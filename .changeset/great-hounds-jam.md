---
'@aether-zone/kosmos': minor
---

Add `AppBar` — the application's top bar, with `AppBarBrand`, `AppBarSection`
and `AppBarTitle`. Static, sticky or fixed; optionally constrained to a centred
column.
